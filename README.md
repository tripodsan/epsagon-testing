# Epsagon Openwhisk Testing

### Create an env file

Copy the `.env.template` to `.env` and edit the `EPSAGON_TOKEN`

```
$ cp .env.template .env
$ vi .env
```

### Create the action

```
$ npm install
$ npm run build
```
